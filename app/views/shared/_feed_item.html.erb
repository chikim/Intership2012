<li id="<%= feed_item.id %>">
  <%= link_to gravatar_for(feed_item.user, size: 50), feed_item.user %>
  <span class="user">
    <%= link_to feed_item.user.name, feed_item.user %>
  </span>

  <%= render 'microposts/single', post: feed_item %>
</li>
